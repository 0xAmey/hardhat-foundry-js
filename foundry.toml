[profile.default]
src = 'src'
out = 'out'
libs = ['lib']

[rpc_endpoints]
goreli = "${GORELI_RPC_URL}"

[etherscan]
goreli = { key = "${ETHERSCAN_API_KEY}" }

# See more config options https://github.com/foundry-rs/foundry/tree/master/config
